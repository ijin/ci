machine:
  environment:
dependencies:
  pre:
  cache_directories:
test:
  pre:
  override:
    - echo 'testing'
  post:
deployment:
  dev:
    branch: master
    commands:
      - |
        echo ${slack_hook}
        echo 0

      - echo ${SLACK_HOOK}
      - echo 1
      - curl -X POST --data-urlencode 'payload={"text": "> and comes from *Terraformer*.", "username": "Terraformer", "icon_url": "https://raw.githubusercontent.com/hashicorp/terraform/master/website/source/assets/images/logo-static.png"}' https://hooks.slack.com/services/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

